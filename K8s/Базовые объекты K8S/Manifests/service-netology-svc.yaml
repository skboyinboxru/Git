apiVersion: v1
kind: Service
metadata:
  name: netology-svc
  labels:
    app: netology-web
spec:
  selector:
    app: netology-web
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
